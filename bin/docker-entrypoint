#!/bin/bash -e

# If we’re starting the Rails server via thruster, prepare the DB first
if [ "$2" = "./bin/rails" ] && [ "$3" = "server" ]; then
  echo "Running db:prepare before starting server..."
  ./bin/rails db:prepare
fi

# Hand off to whatever command Docker/DO passed in
exec "$@"
