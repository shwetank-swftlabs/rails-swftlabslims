<% content_for :page_title, render("shared/page_title",
  subtitle: "Manage chemical types.",
  actions: render("shared/add_button", label: "Add Chemical Type", path: new_admin_chemical_type_path)
) %>

<!-- CHEMICAL TYPES TABLE -->
<%= render "shared/tables/default_table",
      collection: @chemical_types,
      pagy: nil,
      headers: ->(_) do %>
        <th>Name</th>
        <th>Status</th>
        <th>Actions</th>
      <% end,
      row: ->(chemical_type) do %>
        <td class="fw-semibold"><%= chemical_type.name %></td>
        <td>
          <% if chemical_type.is_active? %>
            <span class="badge bg-success">
              <i class="bi bi-check-circle me-1"></i>Active
            </span>
          <% else %>
            <span class="badge bg-danger">
              <i class="bi bi-x-circle me-1"></i>Inactive
            </span>
          <% end %>
        </td>
        <td>
          <%= render "shared/edit_button", label: "Edit", path: edit_admin_chemical_type_path(chemical_type), size: "sm" %>
        </td>
      <% end %>

