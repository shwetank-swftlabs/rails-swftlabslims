<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= page_title ? "#{page_title} | SWFTLabsLIMS" : "SWFTLabsLIMS" %>
    </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- Icons -->
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Bootstrap CSS (MUST load before your overrides) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
          crossorigin="anonymous">

    <!-- Rails app.css (your app-wide styles) -->
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>

    <!-- Your custom muted scientific theme overrides (MUST BE LAST) -->
    <link href="<%= asset_path('defaults.css') %>" rel="stylesheet" data-turbo-track="reload">

    <!-- JS importmap -->
    <%= javascript_importmap_tags %>
  </head>

  <body data-controller="na-formatter">
    <%= logged_in? ? render("shared/navbar") : nil %>

    <div class="container mt-3">
      <%= render "shared/breadcrumb" %>
      <%= yield :page_title %>
      <%= render "shared/notice" if flash.present? && controller_name != "sessions" %>
      <%= yield %>
    </div>

    <!-- Bootstrap bundle JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
            crossorigin="anonymous"></script>
  </body>
</html>
