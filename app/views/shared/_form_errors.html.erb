<% if object.present? && object.errors.any? %>
  <div class="alert alert-danger">
    <h5 class="alert-heading">There were some errors:</h5>
    <ul class="mb-0">
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
